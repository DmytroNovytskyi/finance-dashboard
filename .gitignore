# build output
backend/build/
backend/.gradle/
frontend/node_modules/
frontend/dist/

# local dev workspace & secrets (never committed)
local/
.env

# IDE (IntelliJ — project-specific, not a generic catch-all)
.idea/
*.iml
