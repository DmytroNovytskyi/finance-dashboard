import Typography from '@mui/material/Typography'

/** Transactions list with filters, category drill-down, and delete-by-range. */
export function TransactionsPage() {
  return <Typography variant="h4">Transactions</Typography>
}
