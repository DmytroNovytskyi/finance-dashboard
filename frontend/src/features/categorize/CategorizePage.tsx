import Typography from '@mui/material/Typography'

/** Uncategorized queue with bulk assignment and the category manager. */
export function CategorizePage() {
  return <Typography variant="h4">Categorize</Typography>
}
