import Typography from '@mui/material/Typography'

/** Dashboard page: period KPIs, spend trend, category breakdown, top merchants. */
export function OverviewPage() {
  return <Typography variant="h4">Overview</Typography>
}
