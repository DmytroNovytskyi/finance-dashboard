import Typography from '@mui/material/Typography'

/** Statement upload for one account, with the imported-statements list. */
export function ImportPage() {
  return <Typography variant="h4">Import</Typography>
}
